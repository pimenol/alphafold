# EvoTTT experiment — more TTT steps with lower LR
# Tests whether longer training with slower LR helps

model_name: model_1_ptm

ttt_steps: 200
ttt_lr: 1.0e-4
lora_rank: 4
last_n_blocks: 8
lora_alpha: 1.0
grad_clip: 1.0
mask_fraction: 0.15

seed: 0
skip_existing: true
