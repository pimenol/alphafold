# EvoTTT experiment — higher LoRA rank
# Tests whether more LoRA capacity improves adaptation

model_name: model_1_ptm

ttt_steps: 50
ttt_lr: 3.0e-4
lora_rank: 16
last_n_blocks: 8
lora_alpha: 1.0

mask_fraction: 0.15

seed: 0
skip_existing: true
